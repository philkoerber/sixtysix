import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
    fonts: {
        cardFont: "Abril Fatface, serif",
        bodyFont: "Lato, sans-serif"
    }
})

export default theme;